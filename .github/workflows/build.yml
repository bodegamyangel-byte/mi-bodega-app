name: Compilar APK de Mi Bodega

on:
  push:
    branches: [ "main" ]
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-22.04

    steps:
    - name: Checkout del código
      uses: actions/checkout@v4

    - name: Configurar Java 17
      uses: actions/setup-java@v4
      with:
        distribution: 'temurin'
        java-version: '17'

    - name: Instalar dependencias del sistema
      run: |
        sudo apt-get update
        sudo apt-get install -y libncurses5 libncursesw5-dev libtinfo5 cmake libffi-dev libssl-dev

    - name: Instalar Buildozer y Cython
      run: |
        pip3 install --user --upgrade buildozer cython==0.29.33

    - name: Compilar con Buildozer (Pre-descarga)
      run: |
        export PATH=$PATH:~/.local/bin
        # Intentamos compilar una vez, fallará, pero descargará todo
        buildozer android debug || true

    - name: Forzar ubicación de SDK Manager
      run: |
        # Creamos la estructura que Buildozer necesita desesperadamente
        mkdir -p $HOME/.buildozer/android/platform/android-sdk/tools/bin
        
        # Copiamos todo lo de cmdline-tools a la carpeta donde Buildozer lo busca
        if [ -d "$HOME/.buildozer/android/platform/android-sdk/cmdline-tools/latest/bin" ]; then
           cp -r $HOME/.buildozer/android/platform/android-sdk/cmdline-tools/latest/bin/* $HOME/.buildozer/android/platform/android-sdk/tools/bin/
        fi
        
        # Damos permisos de ejecución
        chmod +x $HOME/.buildozer/android/platform/android-sdk/tools/bin/sdkmanager

    - name: Compilar (Intento Final)
      run: |
        export PATH=$PATH:~/.local/bin
        buildozer android debug

    - name: Guardar el archivo APK resultante
      uses: actions/upload-artifact@v4
      with:
        name: Mi-Bodega-APK
        path: bin/*.apk